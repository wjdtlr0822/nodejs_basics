import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-flash-message',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
